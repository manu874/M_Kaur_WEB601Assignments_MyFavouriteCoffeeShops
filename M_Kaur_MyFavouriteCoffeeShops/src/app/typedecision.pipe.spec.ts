import { TypedecisionPipe } from './typedecision.pipe';

describe('TypedecisionPipe', () => {
  it('create an instance', () => {
    const pipe = new TypedecisionPipe();
    expect(pipe).toBeTruthy();
  });
});
